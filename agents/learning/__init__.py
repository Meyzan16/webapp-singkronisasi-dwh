"""
Learning agent — future home for:
  - Strategy optimizer (tune StyleConfig weights based on win rate)
  - Signal quality scorer (ML model trained on historical TP/SL outcomes)
  - Adaptive threshold tuner (auto-adjust min_score, min_rr per market regime)
  - Backtester (replay historical data to validate TA engine changes)
"""
