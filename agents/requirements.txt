# Agents dependencies — trading agents that run independently from the API server
# Install: pip install -r agents/requirements.txt

# HTTP client (Binance API calls)
httpx>=0.27

# Database (shared PostgreSQL with backend)
sqlalchemy>=2.0
asyncpg>=0.29

# Config
pydantic-settings>=2.0
python-dotenv>=1.0

# Logging
structlog>=24.0

# ── Future: Learning Agent dependencies ──────────────────────────────────────
# Uncomment as needed:
# scikit-learn>=1.4       # Strategy optimizer, signal scorer
# pandas>=2.0             # Backtester, data analysis
# numpy>=1.26             # Numerical computations
# ta-lib>=0.4             # Additional TA indicators
# torch>=2.0              # Deep learning signal predictor (optional)
# optuna>=3.0             # Hyperparameter tuning for StyleConfig
